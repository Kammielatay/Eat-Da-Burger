<h1 class="text-center">Welcome to Eat-Da-Burger!</h1>

<img src="assets/img/burger.png" class="rounded mx-auto d-block" alt="Photo-of-Burger">

<div class="container text-center">
  <div class="row gx-5">
    <div class="col ">
      <ul>
        {{#each burger}}
          {{#unless devoured}}
            {{> burger/burger-block devoured=true}}
          {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col">
      hkjigigo
      {{!-- <ul>
        {{#each burger}}
          {{#unless devoured}}
            {{> burger/burger-block devoured=false}}
          {{/unless}}
        {{/each}}
      </ul> --}}
    </div>
  </div>




<div id="text-area" class="mb-3 rounded mx-auto d-block">
  <textarea class="form-control" id="burgerInput" rows="2"></textarea>
  <label for="burgerInput" class="form-label text-center">What Kind Of Burger Would You Like To Try Today?</label>
  <br>
  <button class="text-center rounded mx-auto d-block">Submit!</button>
</div>


